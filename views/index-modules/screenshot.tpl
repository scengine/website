{if image}
	<div class="media center">
		<a href="{link}">
			<img src="{image}"
			     alt="{description}"
			     style="max-width: 100%;" />
		</a>
	</div>
{else}
	<p>No screenshot to display</p>
{end}
